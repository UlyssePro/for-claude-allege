# Local dev defaults — placeholders so the app boots without credentials.
# Real secrets (if any are added later) are delivered via /run/base44/app.env
# and override these because that file is listed last in compose env_file.

DATABASE_URL=file:./dev.db
AUTH_SECRET=change-me
